1、设置波特率 
	#define BAUDRATE 250000

2、该母板型号 
	#define MOTHERBOARD 33   单喷头--33    双喷头--34

3、温度传感器种类
	#define TEMP_SENSOR_0 1      喷头  NTC 100K--1
    	#define TEMP_SENSOR_1 0
    	#define TEMP_SENSOR_2 0
    	#define TEMP_SENSOR_BED 1    热床

4、最低挤出温度 
	#define EXTRUDE_MINTEMP 170

5、设置12864
	#define REPRAP_DISCOUNT_FULL_GRAPHIC_SMART_CONTROLLER

	PS：出现错误：In file included from /dogm_lcd_implementation.h:38、、、、、

	解决方法：ArduinoAddons->Arduino_1.x.x->libraries下面的U8glib目录需要拷贝到IDE目录->libraries下		 http://www.makerlab.me/t/48

6、限位开关的设置 
	const bool X_MIN_ENDSTOP_INVERTING = true; 
	const bool Y_MIN_ENDSTOP_INVERTING = true;
	const bool Z_MIN_ENDSTOP_INVERTING = true;
	const bool X_MAX_ENDSTOP_INVERTING = true; 
	const bool Y_MAX_ENDSTOP_INVERTING = true; 
	const bool Z_MAX_ENDSTOP_INVERTING = true; 
        
	常开--true     常闭--false

#define DEFAULT_AXIS_STEPS_PER_UNIT   {53.3333,53.3333,200.0*8/3,100}  // default steps per unit for Ultimaker






